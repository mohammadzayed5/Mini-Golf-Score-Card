Flask==3.0.3
flask-cors==4.0.1 #this allows cross-origin requests from my app (IPHONE!)
gunicorn==23.0.0
PyJWT==2.8.0
python-dotenv==1.1.1
requests==2.32.3 #Talks to Turso's /v2/pipeline HTTP API. Replaced the libsql Python package because its tokio runtime deadlocks under gunicorn's threaded worker.