Flask==3.0.3
flask-cors==4.0.1 #this allows cross-origin requests from my app (IPHONE!)
gunicorn==23.0.0
PyJWT==2.8.0
python-dotenv==1.1.1
libsql==0.1.8 #Turso client - persistent DB (Render free tier disk is ephemeral). Pinned exactly: 0.1.11 hangs on cursor.execute() under gunicorn threads.